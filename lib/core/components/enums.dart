enum TransitionDirection {
  leftToRight,
  rightToLeft,
  topToBottom,
  bottomToTop,
}

enum KeyboardAction {
  select,
  upgrade,
  refuel,
  shoot,
  move,
  none,
}

enum NextGalaxy {
  top,
  bottom,
  left,
  right,
}

enum SpaceTilePosition {
  center,
  top,
  bottom,
  left,
  right,
}